{
  "framework": null,
  "buildCommand": "cd chat-ui && npm install && npm run build",
  "outputDirectory": "chat-ui/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ]
}
